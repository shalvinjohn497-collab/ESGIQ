/**
 * @deprecated BRD §12 — use `evaluateInsights` from `@/calculations/insights/evaluateInsights`
 * and read `insights` from the assessment store (populated by `useAssessmentResults`).
 * This stub remains so legacy imports do not break the build.
 */

export function generateInsights() {
    return [];
}

export default generateInsights;
